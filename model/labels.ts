type labels = {
    id: string; // SurrealDB record ID
    name: string;
    description: string;
    colour: string; // Hex colour code, e.g., "#FF5733"
    createdAt: string; // ISO 8601 timestamp
    updatedAt: string; // ISO 8601 timestamp
}